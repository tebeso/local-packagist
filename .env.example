# User and group IDs for the Docker container
# These should match your local user's UID and GID to avoid permission issues
# You can find your user ID and group ID by running the command: id -u && id -g

# User ID (default: 1000)
USER_ID=1000

# Group ID (default: 1000)
GROUP_ID=1000